/**
 * Variables
 * ======================================================================== */

block_storage('__stem__grid-settings', {
        columns:     24,
        gutter:      20px,
        error:       5,
        container:   {
                xs:  null,
                sm:  720px,
                md:  940px,
            },
        width:       940px,

        space:       6px,

        size: {
            none:    0
            sm:      2
            md:      3
            lg:      5
            xl:      8
            giant:   12
            mega:    18
            giga:    24
        }

        responsive:  true
    }, true)


/**
 * Mixins
 * ======================================================================== */

grid-settings(data=false, default=false, fallback=false)
    temp = block_storage('__stem__grid-settings', data, default, fallback)

    return temp